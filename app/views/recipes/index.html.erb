<h1>Recipe Index</h1>

<% if session[:user_id]%>
    <%= link_to "New", new_recipe_path%>
<% end %>

<% @recipes.each do |recipe|%>
    <h1><%= recipe.name %></h1>
    <img src=<%=recipe.image%>>
    <%= link_to 'Details', recipe%>
<% end %>