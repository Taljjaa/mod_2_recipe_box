<div class="flexbox_col">
    <h1>Recipe Index</h1>
    <%= form_tag(recipes_path, method: :get) do %>
        <%= label_tag(:q, "Search:") %>
        <%= text_field_tag :search %>
        <%= submit_tag("Search") %>
    <% end %>
    <button><%= link_to "Surprise Me", surprise_me_path%></button>
    <% @recipes.each do |recipe|%>
        <h1><%= recipe.name %></h1>
        <%= link_to 'Details', recipe%>
        <img src=<%=recipe.image%>>
    <% end %>
</div>

